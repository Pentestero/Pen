# Server configuration
server.port=8080

# Database Configuration (PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/messagerie_db
spring.datasource.username=postgres
spring.datasource.password=sikatipierre5.0
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA and Hibernate Configuration
# 'update' will create tables if they don't exist and update schema if entities change.
# For production, consider 'validate' or 'none' after initial setup.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true 
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# WebSocket Configuration (assuming it runs on the same port as the HTTP server)
spring.websocket.server.port=8080

# Logging configuration (optional, for more detailed logs if needed)
# logging.level.org.springframework.web=DEBUG
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE